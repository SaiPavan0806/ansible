- name: install packages
  hosts: local
  connection: local
  become: yes
  tasks:
    - name: install multiple packages
      ansible.builtin.package:
        name: "{{ item }}"
        state: present
      loop:
        - nginx
        - mysql
        - mongodb